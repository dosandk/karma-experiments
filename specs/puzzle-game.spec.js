import {
  createTiles,
  shuffleTiles,
  insertTiles
} from '../src/puzzle-game';

describe('puzzle-game::', () => {
  console.error('createTiles', createTiles);
  console.error('shuffleTiles', shuffleTiles);
  console.error('insertTiles', insertTiles);
});
